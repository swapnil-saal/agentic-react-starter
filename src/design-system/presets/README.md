# Brand presets

Each file here is a complete replacement for the `:root` block in
`../brand.css`. Copy one over it to adopt that look wholesale, then tune.

The components never change — only these numbers do.

| Preset          | Feel                                                           |
| --------------- | -------------------------------------------------------------- |
| `deep-sea.css`  | The default. Teal, crisp corners, calm motion.                 |
| `brutalist.css` | Sharp corners, heavy borders, zero shadow, instant motion.     |
| `soft.css`      | Rounded, roomy, warm, gentle. Marketing and consumer apps.     |
| `technical.css` | Dense, small radius, cool blue. Dashboards and data tools.     |
| `mono.css`      | Greyscale, monospaced, hairline borders. Documentation, tools. |

Try them live at `/brand` in the running app — it can load each preset and
show you the whole UI under it before you commit.
