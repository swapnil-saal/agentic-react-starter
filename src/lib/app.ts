/**
 * The application's display name.
 *
 * Injected at build time from the `name` field in package.json (see
 * vite.config.ts), so the project has exactly one name. Rename the package and
 * the sidebar, header and browser title all follow — nothing to keep in sync.
 */
export const APP_NAME: string = __APP_NAME__
