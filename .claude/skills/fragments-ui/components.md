<!-- GENERATED by scripts/gen-catalog.mjs — do not edit by hand. -->
<!-- Source: @usefragments/ui@2.0.4 -->

# Fragments component catalogue (@usefragments/ui@2.0.4)

70 components. 64 ship a machine-readable contract.

Import everything from the package root unless the component is listed
under "Subpath imports" at the bottom:

```tsx
import { Button, Card, Stack } from '@usefragments/ui'
```

## Index by category

**actions** — ButtonGroup, IconButton, ToggleGroup

**ai** — ConversationList, Message, Prompt, ThinkingIndicator

**display** — Avatar, Badge, Chart, CodeBlock, Icon, Image, List, Markdown, Table, Text

**feedback** — Alert, EmptyState, Loading, Progress, Skeleton, Toast

**inputs** — Checkbox, Chip, ColorPicker, Combobox, DatePicker, Editor, Field, Fieldset, Form, Input, Listbox, RadioGroup, Slider, Switch, Textarea

**layout** — Accordion, AppShell, BentoGrid, Box, Collapsible, Grid, Header, Main, ScrollArea, Separator, Sidebar, Stack

**navigation** — Breadcrumbs, Command, Link, NavigationMenu, Pagination, TableOfContents, Tabs

**other** — Button, Card, DataTable, Dialog, Select, ThemeToggle

**overlays** — Drawer, Menu, Popover, Tooltip

**utilities** — ComponentDefaults, Theme, VisuallyHidden

---

## Components

### Accordion

Stacks sections that expand one at a time, or several at once.

_category: layout · status: stable · tags: accordion, collapse, expand, disclosure, faq_

```
type: single|multiple (default: single)
value: union
defaultValue: union
onValueChange: union
children: node (required)
collapsible: boolean (default: false)
headingLevel: union (default: 3)
```

Full contract: `node_modules/@usefragments/ui/src/components/Accordion/Accordion.contract.json`

### Alert

Tells the user what just happened, or what is about to.

_category: feedback · status: stable · tags: notification, message, feedback, banner, toast_

```
children: node (required)
tone: info|success|warning|danger (default: info)
emphasis: tint|surface (default: tint)
```

Full contract: `node_modules/@usefragments/ui/src/components/Alert/Alert.contract.json`

### AppShell

Lays out header, sidebar, main, and optional aside as one responsive app frame.

_category: layout · status: stable · tags: layout, shell, scaffold, dashboard, app-layout_

```
children: node (required)
layout: default|sidebar|sidebar-floating|floating (default: default)
bg: string
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/AppShell/AppShell.contract.json`

### Avatar

Shows who someone is — their photo, or their initials when there is none.

_category: display · status: stable · tags: user, profile, image, identity_

```
src: string
alt: string (default: )
initials: string
name: string
size: xs|sm|md|lg|xl (default: md)
customSize: union
shape: circle|square (default: circle)
color: string
imageStyle: object
imageProps: object
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Avatar/Avatar.contract.json`

### Badge

Labels a status, count, or category without pulling focus.

_category: display · status: stable · tags: status, label, tag, count, chip_

```
children: node (required)
variant: soft|outline|ghost (default: soft)
tone: neutral|accent|info|success|warning|danger (default: neutral)
size: sm|md|lg (default: md)
dot: boolean (default: false)
dotPulse: boolean (default: false)
dotColor: string
icon: node
active: boolean (default: false)
onRemove: function
announce: boolean (default: false)
role: union
```

Full contract: `node_modules/@usefragments/ui/src/components/Badge/Badge.contract.json`

### BentoGrid

Asymmetric feature grid where tiles span different widths and heights.

_category: layout · status: stable · tags: bento, grid, layout, responsive, feature-grid, asymmetric_

```
children: node
columns: union (default: 3)
gap: none|xs|sm|md|lg|xl (default: md)
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/BentoGrid/BentoGrid.contract.json`

### Box

Applies spacing, background, border, and radius to any element.

_category: layout · status: stable · tags: layout, container, spacing, primitive, box_

```
children: node
as: div|section|article|aside|main|header|footer|nav|span (default: div)
padding: none|xs|sm|md|lg|xl
paddingX: none|xs|sm|md|lg|xl
paddingY: none|xs|sm|md|lg|xl
margin: none|xs|sm|md|lg|xl|auto
marginX: none|xs|sm|md|lg|xl|auto
marginY: none|xs|sm|md|lg|xl|auto
background: none|primary|secondary|tertiary|elevated
rounded: none|sm|md|lg|full
border: boolean
borderTop: boolean
borderBottom: boolean
borderLeft: boolean
borderRight: boolean
borderColor: default|strong|accent|danger
shadow: none|sm|md|lg
overflow: hidden|auto|scroll|visible
color: primary|secondary|tertiary|accent|inverse
display: none|block|inline|inline-block|flex|inline-flex|grid
width: union
minWidth: union
maxWidth: union
height: union
minHeight: union
maxHeight: union
className: string
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Box/Box.contract.json`

### Breadcrumbs

Shows where the user is and links back up the hierarchy.

_category: navigation · status: stable · tags: breadcrumbs, navigation, hierarchy, wayfinding_

```
children: node (required)
separator: node (default: /)
maxItems: number
label: string (default: Breadcrumb)
```

Full contract: `node_modules/@usefragments/ui/src/components/Breadcrumbs/Breadcrumbs.contract.json`

### Button

No contract shipped. Read the typings for exact props:
`node_modules/@usefragments/ui/dist/components/Button/index.d.ts`

### ButtonGroup

Lays out related buttons in a row with one shared gap and alignment.

_category: actions · status: stable · tags: button, group, toolbar, actions, layout_

```
children: node (required)
gap: none|xs|sm|md (default: sm)
wrap: boolean (default: false)
align: start|center|end
```

Full contract: `node_modules/@usefragments/ui/src/components/ButtonGroup/ButtonGroup.contract.json`

### Card

No contract shipped. Read the typings for exact props:
`node_modules/@usefragments/ui/dist/components/Card/index.d.ts`

### Chart

Plots your data with tooltips, legends, and colors that track the theme.

_category: display · status: stable · tags: chart, graph, data-visualization, recharts_

```
config: object (required)
children: element (required)
summary: string
dataTable: node
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Chart/Chart.contract.json`

### Checkbox

Selects options that take effect when a form is submitted.

_category: inputs · status: stable · tags: checkbox, form, boolean, selection, input_

```
checked: boolean
defaultChecked: boolean
onCheckedChange: function
onChange: function
indeterminate: boolean (default: false)
disabled: boolean (default: false)
required: boolean (default: false)
size: sm|md|lg (default: md)
variant: outline
label: string
helperText: string
description: string
name: string
value: string
id: string
controlClassName: string
contentClassName: string
aria-label: string
aria-labelledby: string
aria-describedby: string
```

Full contract: `node_modules/@usefragments/ui/src/components/Checkbox/Checkbox.contract.json`

### Chip

Pill control for filtering, multi-select, and removable tags.

_category: inputs · status: stable · tags: chip, tag, filter, selection, multi-select, action_

```
children: node (required)
variant: solid|outline|soft (default: solid)
size: xs|sm|md|lg (default: xs)
selected: boolean (default: false)
icon: node
avatar: node
onRemove: function
value: string
disabled: boolean (default: false)
onClick: function
```

Full contract: `node_modules/@usefragments/ui/src/components/Chip/Chip.contract.json`

### CodeBlock

Shows code with syntax colors, a copy button, and diff or collapse when you need them.

_category: display · status: stable · tags: code, syntax, highlighting, documentation, developer, diff_

```
code: string (required)
language: tsx|typescript|ts|javascript|js|jsx|bash|shell|css|scss|sass|json|html|xml|markdown|md|yaml|yml|python|py|ruby|go|rust|java|kotlin|swift|c|cpp|csharp|php|sql|graphql|diff|plaintext|text (default: tsx)
theme: css-variables|synthwave-84|github-dark|github-light|one-dark-pro|dracula|nord|monokai|vitesse-dark|vitesse-light|min-dark|min-light (default: css-variables)
showCopy: boolean (default: true)
title: string
filename: string
caption: string
showLineNumbers: boolean (default: false)
startLineNumber: number (default: 1)
highlightLines: array
addedLines: array
removedLines: array
wordWrap: boolean (default: false)
maxHeight: number
collapsible: boolean (default: false)
defaultCollapsed: boolean (default: false)
collapsedLines: number (default: 5)
compact: boolean (default: false)
persistentCopy: boolean (default: false)
copyPlacement: auto|header|overlay (default: auto)
bg: string
onCopy: function
```

Full contract: `node_modules/@usefragments/ui/src/components/CodeBlock/CodeBlock.contract.json`

### Collapsible

Shows or hides one region of content behind a trigger.

_category: layout · status: stable · tags: collapsible, expandable, accordion, disclosure, toggle_

```
children: node (required)
defaultOpen: boolean (default: false)
open: boolean
onOpenChange: function
disabled: boolean (default: false)
```

Full contract: `node_modules/@usefragments/ui/src/components/Collapsible/Collapsible.contract.json`

### ColorPicker

Selects a color through a visual picker and hexadecimal input.

_category: inputs · status: stable · tags: color, picker, input, hex, swatch, theme_

```
label: string
value: string
defaultValue: string (default: #000000)
onChange: function
onValueChange: function
helperText: string
description: string
error: boolean (default: false)
disabled: boolean (default: false)
size: sm|md|lg (default: md)
showInput: boolean (default: true)
```

Full contract: `node_modules/@usefragments/ui/src/components/ColorPicker/ColorPicker.contract.json`

### Combobox

Filters a list of options as the user types.

_category: inputs · status: stable · tags: combobox, autocomplete, search, select, typeahead, form, multiselect_

```
multiple: boolean (default: false)
value: union
defaultValue: union
onValueChange: union
onChange: union
children: node (required)
open: boolean
defaultOpen: boolean
onOpenChange: function
required: boolean
name: string
placeholder: string
autoHighlight: boolean (default: true)
size: sm|md|lg (default: md)
className: string
label: string
helperText: string
error: union
disabled: boolean
```

Full contract: `node_modules/@usefragments/ui/src/components/Combobox/Combobox.contract.json`

### Command

Filters a list of actions as the user types.

_category: navigation · status: stable · tags: command, palette, search, spotlight, quick-actions, cmdk_

```
children: node (required)
search: string
defaultSearch: string (default: )
onSearchChange: function
filter: function
loop: boolean (default: true)
```

Full contract: `node_modules/@usefragments/ui/src/components/Command/Command.contract.json`

### ComponentDefaults

Utility provider that sets the default control size for every control below it.

_category: utilities · status: stable · tags: provider, defaults, size, utility_

```
children: node (required)
controlSize: sm|md|lg
value: { controlSize?: sm|md|lg }
```

Full contract: `node_modules/@usefragments/ui/src/components/ComponentDefaults/ComponentDefaults.contract.json`

### ConversationList

Stacks chat messages in a scroller that follows new arrivals.

_category: ai · status: stable · tags: conversation, chat, messages, scroll, ai, list_

```
children: node (required)
showAvatars: boolean (default: true)
autoScroll: union (default: smart)
onScrollTop: function
loadingHistory: boolean (default: false)
emptyState: node
scrollTopThreshold: number (default: 50)
scrollBottomThreshold: number (default: 100)
```

Full contract: `node_modules/@usefragments/ui/src/components/ConversationList/ConversationList.contract.json`

### DataTable

No contract shipped. Read the typings for exact props:
`node_modules/@usefragments/ui/dist/components/DataTable/index.d.ts`

### DatePicker

Selects a date or date range from a calendar.

_category: inputs · status: stable · tags: date, picker, calendar, range, form, input_

```
children: node (required)
className: string
mode: single|range (default: single)
selected: object
selectedRange: object
onSelect: function
onChange: function
onRangeSelect: function
numberOfMonths: number (default: 1)
disabled: boolean (default: false)
size: sm|md|lg (default: md)
disabledDates: union
placeholder: string
locale: object
fixedWeeks: boolean (default: false)
formatDate: function
formatRange: function
open: boolean
onOpenChange: function
name: string
label: string
helperText: string
error: union
```

Full contract: `node_modules/@usefragments/ui/src/components/DatePicker/DatePicker.contract.json`

### Dialog

No contract shipped. Read the typings for exact props:
`node_modules/@usefragments/ui/dist/components/Dialog/index.d.ts`

### Drawer

Slides a panel in from a screen edge and dims the page behind it.

_category: overlays · status: stable · tags: drawer, sheet, panel, sidebar, slide-over_

```
children: node (required)
open: boolean
defaultOpen: boolean
onOpenChange: function
modal: boolean (default: true)
```

Full contract: `node_modules/@usefragments/ui/src/components/Drawer/Drawer.contract.json`

### Editor

Edits rich text with formatting, autosave, and a word count.

_category: inputs · status: stable · tags: editor, rich-text, markdown, toolbar, writing, blog, content_

```
children: node
value: string
defaultValue: string (default: )
onValueChange: function
placeholder: string (default: Start typing...)
disabled: boolean (default: false)
readOnly: boolean (default: false)
formats: array
toolbar: boolean (default: true)
statusBar: boolean (default: true)
onAutoSave: function
autoSaveInterval: number (default: 30000)
size: sm|md|lg (default: md)
maxLength: number
toolbarIcons: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Editor/Editor.contract.json`

### EmptyState

Explains why an area is blank and offers the next step.

_category: feedback · status: stable · tags: empty, placeholder, no-data, zero-state, blank-slate_

```
children: node (required)
size: sm|md|lg (default: md)
variant: ghost|outline (default: ghost)
```

Full contract: `node_modules/@usefragments/ui/src/components/EmptyState/EmptyState.contract.json`

### Field

Composes labels, descriptions, validation, and errors around a form control.

_category: inputs · status: stable · tags: form, field, validation, label, error, input, accessible_

```
children: node (required)
name: string
disabled: boolean
invalid: boolean
validate: function
validationMode: onSubmit|onBlur|onChange
validationDebounceTime: number
```

Full contract: `node_modules/@usefragments/ui/src/components/Field/Field.contract.json`

### Fieldset

Groups related form controls under an accessible legend.

_category: inputs · status: stable · tags: form, fieldset, group, legend, accessible_

```
children: node (required)
disabled: boolean
```

Full contract: `node_modules/@usefragments/ui/src/components/Fieldset/Fieldset.contract.json`

### Form

Distributes server-side validation errors to its Field components.

_category: inputs · status: stable · tags: form, validation, errors, submit, server_

```
children: node (required)
errors: object
onSubmit: function
onFormSubmit: function
onClearErrors: function
validationMode: onSubmit|onBlur|onChange
```

Full contract: `node_modules/@usefragments/ui/src/components/Form/Form.contract.json`

### Grid

Arranges items into responsive columns with one shared gap.

_category: layout · status: stable · tags: grid, layout, columns, responsive_

```
children: node
columns: union (default: 1)
minChildWidth: string
gap: union (default: md)
alignItems: start|center|end|stretch
justifyItems: start|center|end|stretch
padding: none|sm|md|lg (default: none)
className: string
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Grid/Grid.contract.json`

### Header

Composes brand, navigation, search, and actions into one responsive app bar.

_category: layout · status: stable · tags: header, navigation, navbar, brand, layout, dropdown_

```
children: node (required)
height: string (default: 56px)
position: static|fixed|sticky (default: static)
elevatedOnScroll: boolean|{ threshold?: number }
navAlign: start|center (default: start)
container: full|page (default: full)
icons: object
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Header/Header.contract.json`

### Icon

Sizes and colours any icon so it lines up with the text beside it.

_category: display · status: stable · tags: icon, visual, symbol, graphic_

```
style: object
icon: union (required)
size: xs|sm|md|lg|xl (default: md)
weight: string (default: regular)
tone: accent|info|success|warning|danger
color: primary|secondary|tertiary
iconProps: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Icon/Icon.contract.json`

### IconButton

Runs an action from an icon alone, for toolbars and table rows.

_category: actions · status: stable · tags: action, button, icon, toolbar, interactive_

```
children: node (required)
variant: ghost|soft|outline (default: ghost)
size: sm|md|lg (default: md)
pressed: boolean
aria-label: string
aria-labelledby: string
```

Full contract: `node_modules/@usefragments/ui/src/components/IconButton/IconButton.contract.json`

### Image

Renders a picture at a fixed shape with a built-in fallback when loading fails.

_category: display · status: stable · tags: image, media, photo, picture, visual_

```
src: string (required)
alt: string (required)
aspectRatio: 1:1|4:3|16:9|21:9|auto (default: auto)
objectFit: cover|contain|fill|none (default: cover)
width: union
height: union
rounded: none|sm|md|lg|full (default: none)
fallback: node
className: string
style: object
imgProps: object
onImageLoad: function
onImageError: function
```

Full contract: `node_modules/@usefragments/ui/src/components/Image/Image.contract.json`

### Input

Collects a single line of user-entered text.

_category: inputs · status: stable · tags: form, input, text_

```
value: string
defaultValue: string
placeholder: string
type: number|text|email|password|tel|url (default: text)
size: sm|md|lg (default: md)
disabled: boolean (default: false)
error: boolean (default: false)
label: string
required: boolean (default: false)
helperText: string
startAdornment: node
endAdornment: node
shortcut: string
shortcutBehavior: display-only|focus-input (default: display-only)
onChange: function
onValueChange: function
onBlur: function
onFocus: function
onKeyDown: function
rootProps: object
inputStyle: object
inputClassName: string
withFieldWrapper: boolean (default: true)
className: string
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Input/Input.contract.json`

### Link

Navigates somewhere else, inline or in chrome, with real link semantics.

_category: navigation · status: stable · tags: link, anchor, navigation, href, url_

```
children: node (required)
tone: accent|neutral (default: accent)
color: primary|secondary|tertiary
underline: always|hover|none|dotted (default: hover)
external: boolean (default: false)
asChild: boolean (default: false)
className: string
style: object
target: union
rel: string
```

Full contract: `node_modules/@usefragments/ui/src/components/Link/Link.contract.json`

### List

Stacks items down the page with evenly spaced bullets, numbers, or icons.

_category: display · status: stable · tags: list, items, bullet, ordered, unordered_

```
children: node (required)
as: ul|ol (default: ul)
marker: none|disc|decimal|icon (default: disc)
gap: none|xs|sm|md|lg|xl (default: sm)
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/List/List.contract.json`

### Listbox

Shows a keyboard-navigable list of options with no trigger of its own.

_category: inputs · status: stable · tags: listbox, search, autocomplete, combobox, command, dropdown_

```
children: node (required)
style: object
tabIndex: number
onFocus: function
onKeyDown: function
```

Full contract: `node_modules/@usefragments/ui/src/components/Listbox/Listbox.contract.json`

### Loading

Signals that something is happening when you cannot say how long it will take.

_category: feedback · status: stable · tags: loading, spinner, progress, feedback, indicator, async_

```
size: sm|md|lg|xl (default: md)
kind: spinner|dots|pulse (default: spinner)
label: string (default: Loading...)
centered: boolean (default: false)
fill: boolean (default: false)
overlay: boolean (default: false)
color: accent|current|muted (default: accent)
```

Full contract: `node_modules/@usefragments/ui/src/components/Loading/Loading.contract.json`

### Main

Owns a page's gutters, reading measure, and vertical rhythm.

_category: layout · status: stable · tags: main, page, layout, compound_

```
children: node (required)
measure: full|narrow (default: full)
as: semantic element (default: main)
```

Full contract: `node_modules/@usefragments/ui/src/components/Main/Main.contract.json`

### Markdown

Turns a Markdown string into styled headings, lists, tables, code, and quotes.

_category: display · status: stable · tags: markdown, prose, content, text, ai, chat_

```
content: string (required)
components: object
className: string
```

Full contract: `node_modules/@usefragments/ui/src/components/Markdown/Markdown.contract.json`

### Menu

Drops a list of actions below a trigger, driveable entirely by keyboard.

_category: overlays · status: stable · tags: menu, dropdown, actions, context-menu, commands, submenu_

```
children: node (required)
open: boolean
defaultOpen: boolean
onOpenChange: function
modal: boolean (default: true)
```

Full contract: `node_modules/@usefragments/ui/src/components/Menu/Menu.contract.json`

### Message

Shows one chat message, styled by who sent it.

_category: ai · status: stable · tags: message, chat, ai, conversation, bubble_

```
role: user|assistant|system (required)
children: node (required)
status: pending|streaming|complete|error (default: complete)
timestamp: object
avatar: node
actions: node
```

Full contract: `node_modules/@usefragments/ui/src/components/Message/Message.contract.json`

### NavigationMenu

Site navigation whose dropdowns become a drawer on small screens.

_category: navigation · status: stable · tags: navigation, menu, header, dropdown, navbar, mobile, responsive, drawer_

```
children: node (required)
value: string
defaultValue: string
onValueChange: function
orientation: horizontal|vertical (default: horizontal)
delayDuration: number (default: 200)
skipDelayDuration: number (default: 300)
icons: object
mobileBreakpoint: md|lg (default: md)
```

Full contract: `node_modules/@usefragments/ui/src/components/NavigationMenu/NavigationMenu.contract.json`

### Pagination

Steps through content split across numbered pages.

_category: navigation · status: stable · tags: pagination, paging, pages, navigation_

```
children: node (required)
totalPages: number (required)
page: number
defaultPage: number (default: 1)
onPageChange: function
edgeCount: number (default: 1)
siblingCount: number (default: 1)
```

Full contract: `node_modules/@usefragments/ui/src/components/Pagination/Pagination.contract.json`

### Popover

Anchors interactive content to a trigger without blocking the page.

_category: overlays · status: stable · tags: popover, overlay, dropdown, floating, contextual_

```
children: node (required)
open: boolean
defaultOpen: boolean
onOpenChange: function
modal: boolean (default: false)
```

Full contract: `node_modules/@usefragments/ui/src/components/Popover/Popover.contract.json`

### Progress

Shows how far along a task is, as a bar or a ring.

_category: feedback · status: stable · tags: progress, loading, indicator, percentage, status_

```
value: number (default: null)
min: number (default: 0)
max: number (default: 100)
size: sm|md|lg (default: md)
tone: accent|neutral|success|warning|danger (default: accent)
role: progressbar|meter (default: progressbar)
label: string
showValue: boolean (default: false)
formatValue: function
```

Full contract: `node_modules/@usefragments/ui/src/components/Progress/Progress.contract.json`

### Prompt

Collects multi-line input for AI and chat interfaces.

_category: ai · status: stable · tags: prompt, chat, ai, input, textarea, form_

```
children: node (required)
value: string
defaultValue: string (default: )
onChange: function
onSubmit: function
placeholder: string (default: Ask, Search or Chat...)
disabled: boolean (default: false)
loading: boolean (default: false)
minRows: number (default: 1)
maxRows: number (default: 8)
autoResize: boolean (default: true)
submitOnEnter: boolean (default: true)
placement: inline|fixed|sticky (default: inline)
variant: outline|ghost (default: outline)
onFiles: function
accept: string
```

Full contract: `node_modules/@usefragments/ui/src/components/Prompt/Prompt.contract.json`

### RadioGroup

Selects one option from a mutually exclusive set.

_category: inputs · status: stable · tags: form, radio, selection, options_

```
value: string
defaultValue: string
onValueChange: function
onChange: function
orientation: horizontal|vertical (default: vertical)
disabled: boolean (default: false)
name: string
label: string
helperText: string
error: union
size: sm|md|lg (default: md)
variant: outline
wrapperClassName: string
groupClassName: string
groupId: string
children: node (required)
```

Full contract: `node_modules/@usefragments/ui/src/components/RadioGroup/RadioGroup.contract.json`

### ScrollArea

Scrolls overflowing content with a slim scrollbar and optional edge fades.

_category: layout · status: stable · tags: scroll, overflow, scrollbar, container, layout_

```
children: node (required)
orientation: horizontal|vertical|both (default: vertical)
scrollbarVisibility: auto|always|hover (default: auto)
showFades: boolean (default: false)
className: string
```

Full contract: `node_modules/@usefragments/ui/src/components/ScrollArea/ScrollArea.contract.json`

### Select

No contract shipped. Read the typings for exact props:
`node_modules/@usefragments/ui/dist/components/Select/index.d.ts`

### Separator

Draws a rule between sections of related content.

_category: layout · status: stable · tags: separator, divider, hr, line, layout_

```
orientation: horizontal|vertical (default: horizontal)
gap: none|xs|sm|md|lg|xl (default: none)
soft: boolean (default: false)
label: string
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Separator/Separator.contract.json`

### Sidebar

Collapsible navigation panel that turns into a drawer on mobile.

_category: layout · status: stable · tags: sidebar, navigation, drawer, menu, layout_

```
children: node (required)
collapsed: boolean
defaultCollapsed: boolean (default: false)
onCollapsedChange: function
open: boolean
defaultOpen: boolean (default: false)
onOpenChange: function
width: string (default: 240px)
collapsedWidth: string (default: 56px)
position: left|right (default: left)
collapsible: icon|offcanvas|none (default: icon)
activeIndicator: start|end (default: start)
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Sidebar/Sidebar.contract.json`

### Skeleton

Holds the shape of content that has not arrived yet.

_category: feedback · status: stable · tags: loading, placeholder, skeleton, shimmer_

```
shape: text|heading|avatar|button|input|rect (default: rect)
size: sm|md|lg (default: md)
width: union
height: union
fill: boolean (default: false)
radius: sm|md|lg|none|full
static: boolean (default: false)
```

Full contract: `node_modules/@usefragments/ui/src/components/Skeleton/Skeleton.contract.json`

### Slider

Selects a numeric value within a defined range.

_category: inputs · status: stable · tags: slider, range, input, number, control_

```
label: string
helperText: string
error: boolean (default: false)
value: number
defaultValue: number
onChange: function
onValueChange: function
min: number (default: 0)
max: number (default: 100)
step: number (default: 1)
showValue: boolean (default: false)
showValueOnDrag: boolean (default: false)
valueSuffix: string (default: )
disabled: boolean (default: false)
name: string
aria-label: string
aria-labelledby: string
aria-describedby: string
id: string
```

Full contract: `node_modules/@usefragments/ui/src/components/Slider/Slider.contract.json`

### Stack

Stacks children in a row or column with one consistent gap.

_category: layout · status: stable · tags: stack, layout, flex, spacing, responsive_

```
children: node (required)
direction: union (default: column)
gap: union (default: md)
align: start|center|end|stretch|baseline
justify: start|center|end|between
wrap: boolean (default: false)
separator: node
as: div|section|nav|article|aside|header|footer|main|ul|ol (default: div)
className: string
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Stack/Stack.contract.json`

### Switch

Toggles a setting that takes effect immediately.

_category: inputs · status: stable · tags: switch, toggle, boolean, settings, preference_

```
checked: boolean
defaultChecked: boolean
onCheckedChange: function
onChange: function
label: string
helperText: string
description: string
disabled: boolean (default: false)
size: sm|md|lg (default: md)
className: string
name: string
id: string
aria-label: string
aria-labelledby: string
aria-describedby: string
```

Full contract: `node_modules/@usefragments/ui/src/components/Switch/Switch.contract.json`

### Table

Renders tabular data as a real HTML table — headers, rows, footer.

_category: display · status: stable · tags: table, semantic, html, layout_

```
size: sm|md (default: md)
striped: boolean (default: false)
bordered: boolean (default: false)
wrapperClassName: string
wrapperProps: object
children: node
```

Full contract: `node_modules/@usefragments/ui/src/components/Table/Table.contract.json`

### TableOfContents

Indexes a long page and marks the section currently in view.

_category: navigation · status: stable · tags: toc, table-of-contents, navigation, sidebar, scroll-spy, headings, tree, hierarchy_

```
children: node (required)
label: string (default: Table of contents)
title: string (default: On This Page)
hideTitle: boolean (default: false)
hideSubItems: boolean (default: false)
```

Full contract: `node_modules/@usefragments/ui/src/components/TableOfContents/TableOfContents.contract.json`

### Tabs

Swaps between sibling panels without leaving the page.

_category: navigation · status: stable · tags: tabs, navigation, panels, content-switcher_

```
children: node (required)
defaultValue: string
value: string
onValueChange: function
orientation: horizontal|vertical (default: horizontal)
variant: ghost|soft (default: ghost)
```

Full contract: `node_modules/@usefragments/ui/src/components/Tabs/Tabs.contract.json`

### Text

Applies the type scale to any text element, without hand-rolling font rules.

_category: display · status: stable · tags: text, typography, heading, paragraph, font_

```
children: node (required)
as: h1|h2|h3|h4|h5|h6|p|span|label|div|strong|em|small|mark|del|ins|sub|sup|time|address|blockquote|cite|code|abbr (default: span)
role: caption|ui-compact|ui-standard|body-compact|body-relaxed|title-sm|title-md|title-lg|code|section-label|eyebrow
scale: 2xs|xs|sm|base|md|lg|xl|2xl|3xl|4xl
weight: normal|medium|semibold|bold (default: "normal")
color: primary|secondary|tertiary|muted|accent|success|warning|danger (default: "primary")
font: sans|mono (default: sans)
truncate: boolean
lineClamp: number
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Text/Text.contract.json`

### Textarea

Collects multi-line text for longer form content.

_category: inputs · status: stable · tags: input, text, form, multiline_

```
value: string
defaultValue: string
placeholder: string
rows: number (default: 3)
minRows: number
maxRows: number
resize: none|vertical|horizontal|both (default: vertical)
size: sm|md|lg (default: md)
disabled: boolean (default: false)
error: boolean (default: false)
showCharCount: boolean (default: false)
label: string
helperText: string
onChange: function
onValueChange: function
onBlur: function
onFocus: function
name: string
maxLength: number
required: boolean
aria-label: string
aria-labelledby: string
aria-describedby: string
rootProps: object
className: string
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Textarea/Textarea.contract.json`

### Theme

Utility provider that gives everything below it a light, dark, or system color mode.

_category: utilities · status: stable · tags: theme, dark-mode, light-mode, provider, toggle_

```
children: node (required)
defaultMode: light|dark|system
defaultTheme: light|dark|system
mode: light|dark|system
onModeChange: function
storageKey: string (default: fui-theme)
attribute: data-theme|class (default: data-theme)
Theme.Toggle size: sm|md|lg
Theme.Button size: sm|md|lg
```

Full contract: `node_modules/@usefragments/ui/src/components/Theme/Theme.contract.json`

### ThemeToggle

No contract shipped. Read the typings for exact props:
`node_modules/@usefragments/ui/dist/components/ThemeToggle/index.d.ts`

### ThinkingIndicator

Shows that an AI response is being generated.

_category: ai · status: stable · tags: thinking, loading, ai, processing, indicator, animation_

```
active: boolean (default: true)
label: string (default: Thinking...)
kind: dots|pulse|spinner (default: dots)
showElapsed: boolean (default: false)
steps: array
```

Full contract: `node_modules/@usefragments/ui/src/components/ThinkingIndicator/ThinkingIndicator.contract.json`

### Toast

Briefly reports what just happened, without blocking the page.

_category: feedback · status: stable · tags: notification, alert, message, feedback_

```
onDismiss: function
title: string
description: string
tone: neutral|success|danger|warning|info (default: neutral)
duration: number
action: object
id: string
onPause: function
onResume: function
```

Full contract: `node_modules/@usefragments/ui/src/components/Toast/Toast.contract.json`

### ToggleGroup

Picks exactly one option from a short row of toggle buttons.

_category: actions · status: stable · tags: toggle, group, fragmented, control, tabs, switch_

```
value: string
defaultValue: string
onChange: function
onValueChange: function
children: node (required)
variant: soft|ghost|outline (default: soft)
size: sm|md|lg (default: md)
gap: sm|none|xs (default: xs for ghost; none otherwise)
selectionMode: single (default: single)
```

Full contract: `node_modules/@usefragments/ui/src/components/ToggleGroup/ToggleGroup.contract.json`

### Tooltip

Shows a one-line hint when a control is hovered or focused.

_category: overlays · status: stable · tags: tooltip, hint, help, hover, contextual_

```
children: element (required)
content: node (required)
side: top|bottom|left|right (default: top)
align: start|center|end (default: center)
sideOffset: number (default: 6)
delay: number
closeDelay: number
disabled: boolean (default: false)
arrow: boolean (default: true)
open: boolean
defaultOpen: boolean
onOpenChange: function
contentProps: object
style: object
```

Full contract: `node_modules/@usefragments/ui/src/components/Tooltip/Tooltip.contract.json`

### VisuallyHidden

Keeps text in the accessibility tree while taking it off the screen.

_category: utilities · status: stable · tags: accessibility, a11y, screen-reader, hidden, sr-only_

```
children: node (required)
as: span|div (default: span)
```

Full contract: `node_modules/@usefragments/ui/src/components/VisuallyHidden/VisuallyHidden.contract.json`

---

## Subpath imports

These components live behind subpath exports and need an optional peer
dependency installed before use. Install the peer when you first import one.

| Import | Peer dependency required |
| --- | --- |
| `@usefragments/ui/chart` | `recharts` |
| `@usefragments/ui/data-table`, `/table` | `@tanstack/react-table`, `@tanstack/react-virtual` |
| `@usefragments/ui/editor` | `@tiptap/react`, `@tiptap/starter-kit`, `@tiptap/extension-link` |
| `@usefragments/ui/datepicker` | `react-day-picker`, `date-fns` |
| `@usefragments/ui/colorpicker` | `react-colorful` |
| `@usefragments/ui/markdown` | `react-markdown`, `remark-gfm` |
| `@usefragments/ui/codeblock` | `shiki` |

They are declared as **optional** peers, so leaving them uninstalled is
supported and produces no warnings.
