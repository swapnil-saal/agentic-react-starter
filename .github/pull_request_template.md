## What and why

<!-- What changes, and what problem it solves. The diff shows what; explain why. -->

## Verification

- [ ] `pnpm check` passes (types, lint, design tokens, unit tests)
- [ ] `pnpm e2e` passes
- [ ] For UI changes: checked at `/brand` with radius 0, motion 0 and
      density 1.3 — the change follows all three rather than hardcoding

## Screenshots

<!-- For anything visual, before/after in both light and dark mode. -->
